SPEC.md is the single source of truth for project direction.

All planning happens through SPEC.md amendments — not separate plan documents. New work starts by updating §T (tasks), new constraints go to §C, new invariants to §V, bugs to §B. Implementation follows the spec, not the other way around.

When scope changes mid-session, update SPEC.md first, then implement. Commit spec changes separately from implementation.
